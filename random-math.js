// Random math
const inputNumber = Math.random() * 6;
const rounded = Math.round( inputNumber );
console.log( rounded );
//using for loop
for ( let i = 0; i <= 30; i++ )
{
    const inputNumber = Math.random() * 6;
    const rounded = Math.ceil( inputNumber );
    console.log( rounded );
}
//random
const number = Math.random() *10;
const roundNumber = Math.round(number);
console.log(roundNumber);